{
  "version": 2,
  "buildCommand": "npm install --prefer-offline && ./node_modules/.bin/vite build || npx vite@4.4.5 build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}